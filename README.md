# fiach

fiach is a Wails desktop app for managing game mods.

## Development

Run the app in development mode:

```bash
wails3 dev
```

Build the app for the current platform:

```bash
wails3 task build
```

Build outputs are written to `bin/`.
