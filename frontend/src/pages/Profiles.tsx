import { SectionPlaceholder } from '../components/SectionPlaceholder/SectionPlaceholder';

export const Profiles = () => {
  return (
    <SectionPlaceholder
      title="Profiles"
      description="Create and manage mod loadouts for each game."
    />
  );
};
